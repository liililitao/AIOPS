# 应用告警处理建议：app_alert_pmt_Login_Failed

> **告警概要**  
> 应用 `PMT for S&D` 在非生产环境发生 10 次登录失败记录，触发规则 `app_alert_pmt_Login_Failed`，疑似 API 漏洞探测或随机扫描行为。整体风险评级 **中**，环境风险低，但攻击类型风险中，需优先核查应用层逻辑与权限配置。

---

## 立即核查（0–30分钟）

1. ✅ **确认告警真实性**  
   - 检查日志文件路径：`C:\Users\BOLL\Desktop\aiops\codeProject\1-aiOpsPoc\test_log_app\pmt\app_alert_pmt_Login_Failed.csv`  
   - 验证日志内容是否包含真实用户操作或测试/扫描流量（如 IP、User-Agent、请求频率等字段）

2. ✅ **确认环境属性**  
   - 当前环境为 `Non-Production`，非生产系统。但仍需排查是否存在测试/开发人员误操作或自动化脚本触发。

3. ✅ **核查请求来源与行为模式**  
   - 检查请求日志中 `query=/pmt/api/v1/login` 的调用频率、来源 IP、User-Agent、是否携带参数（如 username/password）  
   - 判断是否为重复登录尝试、暴力破解测试或自动化工具扫描。

---

## 排查步骤（30分钟–2小时）

### 1. 应用日志深度分析
- 使用 Splunk/ELK 或日志平台，聚合分析：
  ```spl
  index=app_s_12month sourcetype=pmt source=sd_log_action detail="登录失败"
  | stats count by _time, client_ip, user, query, action, module
  | sort - _time
  ```
- 查看是否存在：
  - 相同 IP 短时间内高频请求 → 可能为扫描或爆破
  - 不同 IP 但相同 User-Agent 或请求路径 → 可能为自动化工具
  - 空用户名或无效凭证 → 可能为探测或测试

### 2. 服务依赖与接口权限检查
- 核查 `/pmt/api/v1/login` 接口：
  - 是否暴露在公网或非授权网络？
  - 是否有速率限制（Rate Limiting）或登录失败锁定机制？
  - 接口是否在非生产环境开放认证功能？是否应仅限白名单？

### 3. 权限与配置审计
- 确认 `module=SD_PRICE; action=AUTH` 是否为预期行为：
  - 是否有开发/测试人员误操作？
  - 是否有 CI/CD 或自动化测试脚本触发？
- 检查非生产环境用户权限策略是否与生产环境一致（避免“测试即生产”的权限误用）

### 4. 性能与容量影响评估
- 登录失败请求是否对系统造成负载（如数据库连接、认证服务压力）？
- 若非生产环境无防护，建议临时启用登录失败次数限制或 IP 限流（仅限测试用途，非封禁）

---

## 按风险处置（优先级排序）

| 风险项                | 处置建议                                                                 |
|-----------------------|--------------------------------------------------------------------------|
| **API漏洞探测（中）** | 立即核查 `/pmt/api/v1/login` 是否暴露敏感信息或存在未授权访问风险           |
| **随机扫描（低）**    | 分析请求来源，若为自动化工具，建议在非生产环境部署基础访问控制（如 IP 白名单）|
| **非生产环境暴露**    | 确保非生产环境不承载真实业务数据，认证接口应限制访问入口或设置测试账号     |

> ❗**重要提示**：当前无 WAF 封禁或攻击确认证据，**禁止无依据封禁 IP 或调整 WAF 规则**。仅在确认攻击行为后，可协同安全部门补充安全策略。

---

## 修复与预防

### ✅ 短期措施（24小时内）
- 为 `/pmt/api/v1/login` 接口添加基础限流（如 5次/分钟/IP）
- 在非生产环境登录接口前增加测试令牌或环境标识验证
- 记录并归档此次日志，用于后续审计与测试用例补充

### ✅ 中长期措施（1周内）
- 建立非生产环境 API 访问规范，禁止未授权认证接口暴露
- 在 CI/CD 流程中加入安全扫描，识别未授权登录接口
- 修复日志中未记录的客户端信息（如 IP、User-Agent），增强异常追溯能力
- 增加登录失败事件告警阈值（如连续5次失败触发紧急通知）

---

## 升级条件

如出现以下情况，应升级至应用负责人或安全部门：

1. 登录失败次数在 1 小时内超过 50 次  
2. 发现同一 IP 多次尝试不同用户名/密码（暴力破解特征）  
3. 请求携带异常参数或敏感字段（如 SQL 注入、命令注入特征）  
4. 非生产环境接口被用于真实业务数据访问或凭证泄露

---

> 📝 **协作建议**  
> 联系应用 owner 确认：  
> - 是否有正在进行的自动化测试或安全扫描？  
> - 该 API 是否应在非生产环境开放认证功能？  
> - 是否需要为测试环境设置独立登录入口？

--- 

**处理完毕后请标记状态：**  
✅ 已核查日志真实性  
✅ 已分析请求行为模式  
✅ 已确认环境与接口策略  
✅ 已建议修复与预防措施  
✅ 如有升级条件，请提交至安全部门
```

---
*🤖 AI 生成 (qwen_qwen3_vl_235b_a22b) · 2026-08-13 16:40:26*