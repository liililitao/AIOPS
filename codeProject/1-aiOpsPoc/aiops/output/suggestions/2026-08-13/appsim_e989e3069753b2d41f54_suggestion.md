# 应用告警处理建议：app_alert_pmt_Login_Failed

## ⚠️ 立即核查

1. **确认环境真实性**  
   当前告警源为 `Non-Production` 环境，且 CMDB 无匹配资产记录，判定为模拟/测试环境告警。请确认是否为功能测试、自动化脚本或压测触发，非真实生产异常。

2. **验证日志来源**  
   根据 `operator_notes`，日志已导出至本地路径：  
   `C:\Users\BOLL\Desktop\aiops\codeProject\1-aiOpsPoc\test_log_app\pmt\app_alert_pmt_Login_Failed.csv`  
   → 建议立即读取该文件，验证登录失败记录是否为脚本/测试用户触发。

3. **检查请求路径合理性**  
   请求路径为 `/pmt/sd/authenticate`，模块为 `SD_CUSTOMER`，动作 `VERIFY` —— 需确认该接口是否允许外部/非生产流量调用，或是否配置了测试账号白名单。

---

## 🔍 排查步骤

### 1. 应用日志深度核查
- 从原始日志中提取：
  - 客户端 IP、User-Agent、来源 referer
  - 请求时间戳分布（是否存在高频/集中失败）
  - 请求参数内容（如用户名、验证码、设备指纹等）
- 使用 Splunk 或 ELK 工具执行完整查询，补全 `table time, action, query, detail, module, _raw` 字段，确认是否有异常参数注入或暴力破解特征。

### 2. 服务依赖与接口状态
- 检查 `/pmt/sd/authenticate` 接口所依赖的：
  - 用户认证服务是否正常
  - 数据库连接池是否健康
  - 第三方认证源（如 LDAP、OAuth）是否响应超时或返回异常
- 在非生产环境模拟登录请求，确认是否复现失败（排除配置错误或参数校验过严）。

### 3. 权限与账号审计
- 确认失败请求使用的账号是否为：
  - 测试专用账号（如 test_user_001）
  - 过期/冻结/无权限账号
- 检查应用权限配置，确认是否允许 `SD_CUSTOMER` 模块下 `VERIFY` 动作在当前环境执行。

### 4. 性能与容量监控
- 检查应用服务器 CPU、内存、线程池状态，排除因资源瓶颈导致“伪登录失败”。
- 查看应用日志中是否存在 `AuthenticationException`、`TimeoutException` 等堆栈信息。

---

## 🛡️ 按风险处置（当前整体风险：低）

- ✅ **无需封禁 IP 或调整 WAF 规则**：  
  告警为“应用异常”非攻击特征，且判定为 `random_scan`（随机扫描），非定向攻击。在非生产环境中，此行为可能是自动化工具或误配测试脚本引发。

- ✅ **无需安全事件上报**：  
  无敏感数据泄露、未登录成功、无横向移动特征，不符合安全事件上报标准。

- 🔧 **建议优化日志埋点**：  
  在登录失败日志中补充 `username`（脱敏）、`client_ip`、`reason_code`（如密码错误/账号不存在/验证码过期），便于后续精准溯源。

---

## 🛠️ 修复与预防

### 应用层修复
- 若为测试账号触发 → 建议：
  - 统一使用测试账号前缀（如 `test_`）
  - 在非生产环境设置登录失败日志级别为 `INFO`，不触发告警
- 若为接口配置错误 → 检查 `/pmt/sd/authenticate` 的请求参数校验逻辑，避免因缺失参数导致“登录失败”

### 告警规则优化
- 修改原告警规则 `#5：app_alert_pmt_Login_Failed`，增加环境过滤条件：
  ```spl
  index=app_s_12month sourcetype=pmt source=sd_log_action detail=登录失败
  | search NOT environment=Non-Production
  ```
- 或增加“失败次数阈值+时间窗口”限制（如：10分钟内>100次失败才告警），避免测试干扰。

### 环境管理建议
- 补充 CMDB 资产信息：为 `pmt-for-s&d.simulated.local` 建立非生产环境资产卡片，设置“模拟/测试”标签，便于后续告警过滤与归类。

---

## 📈 升级条件（若触发以下任一情形，升级至二级响应）

- 登录失败数量在 5 分钟内突增至 >100 次
- 出现相同 IP 多账号爆破、或相同账号多 IP 尝试
- 登录成功后伴随异常操作（如访问敏感接口、大量数据导出）
- 生产环境（`Production`）触发相同告警
- 从日志中发现 SQL 注入、XSS、路径遍历等攻击特征

---
> 📌 **处理人建议**：联系该应用开发负责人或测试团队，确认该登录失败是否属预期行为（如自动化测试脚本）。若为误告，可调整告警规则或添加白名单。
```

---
*🤖 AI 生成 (qwen_qwen3_vl_235b_a22b) · 2026-08-13 16:45:23*